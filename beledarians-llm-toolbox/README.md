# Code Sandbox

Give LLM the ability to run JavaScript/TypeScript and Python code in a sandboxed environment.

### Development

The source code resides in the `src/` directory. For development purposes, you can run the plugin in development mode using:

```bash
lms dev
```
